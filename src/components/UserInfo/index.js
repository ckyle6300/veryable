import React from 'react';
import styles from './userInfo.module.css'

const UserInfo = ({ info }) => {
  console.log(info)
  return (
    <div>
      Is this working?
    </div>
  )
}

export default UserInfo;